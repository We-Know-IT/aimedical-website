import React from "react";
import styles from "./privacy-policy-content.module.css";

// The HTML and CSS is generated from a document tool such as google docs or similar which is why it is unreadable.

export default function PrivacyPolicyContent() {
  return (
    <div
      className={
        `c19 doc-content`
          .split(" ")
          .map((c) => styles[c])
          .join(" ") + " bg-red-700"
      }>
      <div>
        <p
          className={`p c5 c9`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
          />
        </p>
      </div>
      <p
        className={`p c55`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c18`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Privacy Policy
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Last updated: 2023-08-04
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c10 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Please note: we are currently reviewing our privacy policy to protect
          our users and their data better. We will be updating it as more
          information becomes available.
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c39`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Contents{" "}
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.zcas3kd45mwt">
            1. Who we are&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.6bpeno8vd9sq">
            1.1 This Privacy
            Policy&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.islm4n8r3xzn">
            1.2 The Services&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.a9qzk0spk52m">
            1.3 Controller&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.f5jeq4ze0p95">
            2. The Personal Data we process about
            you&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;2
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.jnk1h3w294n5">
            3. How we collect personal
            data&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;3
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.nwh4pm4vzlk3">
            3.1 Information provided by
            customers&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;3
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.2xhziffyybma">
            3.2 Information we automatically collect about you and your
            device&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;3
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.1yzlg8omv12e">
            3.3 Information we receive from
            suppliers&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;3
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.ooyv1w74u2gv">
            4. How we use your Personal
            Data&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.hjiy14ul9igr">
            4.1 To enable and provide the
            Services&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.h4c2ehof11p3">
            4.1.1 General&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.geq3tmgkcw8p">
            4.1.2 To administer the Services and our relationship with
            you&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.hbeh2ouyphfv">
            4.1.3 To provide decision support for cancer
            diagnostics&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.55h5vo850mu8">
            4.1.4 To secure the quality and develop the
            Services&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.gx2ol92wd3qc">
            4.1.5 To communicate with you and provide customer
            support&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.dgv8t4pn6uty">
            4.2 To conduct
            research&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;4
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.lz4hy9j205yp">
            4.3 Marketing&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;5
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.xuolsd7ujwp">
            4.3.1 Marketing
            Communication&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;5
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.t6yrd4372ng9">
            4.3.2 Surveys and
            interviews&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;5
          </a>
        </span>
      </p>
      <p
        className={`p c24 c30`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.gu5j8pydmj6p">
            4.3.3 Marketing
            opt-out&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;5
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.inbe7un5s8tm">
            4.4 To comply with legal
            obligations&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;5
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.gorb7fjwjeii">
            5. How long do AIM keep your personal
            data&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;6
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.r73uluj9ar7i">
            6. Disclosures of your Personal
            Data&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;6
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.awtc36j6ehmm">
            6.1 Recipients&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;6
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.iuxw7ur5kbyb">
            6. How we protect your Personal
            Data&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;7
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.g7201dc2hs0n">
            7. Third-party
            links&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;7
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.7blxufhl9l9o">
            8. Your rights in relation to your personal
            data&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;7
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.3xur2wt9zsz">
            8.1 Your rights&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;7
          </a>
        </span>
      </p>
      <p
        className={`p c22`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c3`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.8qnylucknc3c">
            8.2 How to exercise your
            rights&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;8
          </a>
        </span>
      </p>
      <p
        className={`p c24`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c11`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <a
            className={`c12`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}
            href="#h.e7jkq75l62dx">
            9. Glossary&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;8
          </a>
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <h2
        className={`h2 c23 c31 c36`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.xmsgd3l59mx7">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </h2>
      <h2
        className={`h2 c23 c31`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.zcas3kd45mwt">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          1. Who we are
        </span>
      </h2>
      <h3
        className={`h3 c23 c31`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.6bpeno8vd9sq">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          1.1 This Privacy Policy
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          This Privacy Policy covers how AI Medical Technology AB ("AIM", "we",
          "us", or "our") collects and processes your Personal Data (defined in
          section 2) obtained by us through your use of our services or
          otherwise shared by you. It is important that you read it together
          with any other privacy notice that we may provide when we are
          collecting your Personal Data so that you are fully aware of how and
          why we are using it.
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span>
          We keep our Privacy Policy under regular review, and it may be subject
          to change. Any changes will be posted on our website (the "Website")
          and, where appropriate, notified to you.
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.islm4n8r3xzn">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          1.2 The Services
        </span>
      </h3>
      <p
        className={`p c5 c23`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          The service (Dermaliser) is delivered as an application (referred to
          as the "App", "our services", or "the service"),{" "}
        </span>
        <span>
          Dermalyser is a diagnostic decision support system (DDSS) empowered
          with advanced artificial intelligence (AI). The primary function is to
          classify skin cancer such as malignant melanoma using image analysis
          combined with deep learning (AI).{" "}
        </span>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          In addition to the App, AIM has{" "}
        </span>
        <span>a</span>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          &nbsp;Website containing information about the company and its
          products.
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.a9qzk0spk52m">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          1.3 Controller
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM is a controller and is responsible for the processing of your
          Personal Data. We care about the confidentiality and integrity of the
          information that is shared with us and will only process your Personal
          Data in accordance with this Privacy Policy and applicable laws and
          regulations.
        </span>
      </p>
      <p
        className={`p c23 c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM has appointed a Data Protection Officer ("DPO"). If you have any
          questions or comments about this Privacy Policy or the processing of
          your Personal Data, please contact our customer support at{" "}
        </span>
        <span
          className={`c10 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          support@aimedtech.com.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.f5jeq4ze0p95">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          2. The Personal Data we process about you
        </span>
      </h2>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AI Medical Technology AB may process the following types of personal
          data:
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <a id="t.e9e2bf02485118e6dabf6639069f449f7d292031" />
      <a id="t.0" />
      <table
        className={`c58`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <tbody>
          <tr
            className={`c43`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Account Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Includes the registration date, contact data and whether you
                  have an active subscription or not.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c40`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Customer Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Includes address, billing address, delivery address, email
                  address, contact person and telephone number.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c32`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Device Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Includes device identifier, your mobile operating system and
                  OS version.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c38`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  IP Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  The IP address that you are accessing the service from.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c53`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Identity Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  User credentials and digital identity of the user.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c50`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Data related to the patient
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Images of skin leisures with anonymised reference to the
                  customer's patient journaling system.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c56`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Transaction Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  includes details about purchases and payments, excluding bank
                  and credit card details.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c59`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Usage Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Includes details of your use of the Services, such as traffic
                  data and data logs for application monitoring, security and
                  statistics.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c48`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c0`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  User Data
                </span>
              </p>
            </td>
            <td
              className={`c16`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c4`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Includes data the customer provides when setting up an account
                  with Dermalyser for using the App, such as Contact,
                  organisation and Identity.
                </span>
              </p>
            </td>
          </tr>
        </tbody>
      </table>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM also use cookies and browser local storage to distinguish you from
          other service users and to store your preferences.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.jnk1h3w294n5">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          3. How we collect personal data
        </span>
      </h2>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.nwh4pm4vzlk3">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          3.1 Information provided by customers
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM store and process personal data provided by customers when
          registering and signing up to use the App. Information can also be
          gathered from customers answering surveys, contacting our customer
          support or otherwise corresponding or interacting with us and our
          Services.
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          When signing up for the App (by purchasing the app or signing a
          contract), you will be requested to consent to our use of personal
          data (please note that you will need to consent for the App to work).
          You can withdraw your consent at any time by cancelling the agreement
          (according to the terms) or by contacting us at support@aimedtech.com.
          If you provide sensitive data to us by other means than the app – for
          example, via support – this is described in greater detail in section
          4.1.
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.2xhziffyybma">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          3.2 Information we automatically collect about you and your device
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          When you are using our Services, we will automatically collect Device,
          IT and Usage Data. Some Usage Data is collected by using cookies.
          Please see our Cookie Policy for additional details.
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.1yzlg8omv12e">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          3.3 Information we receive from suppliers
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          We receive Device and Usage Data about you from analytics providers
          such as Google Analytics and Transaction and Contact Data from our
          payment service providers.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.ooyv1w74u2gv">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4. How we use your Personal Data
        </span>
      </h2>
      <h3
        className={`h3 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.hjiy14ul9igr">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.1 To enable and provide the Services
        </span>
      </h3>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.h4c2ehof11p3">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.1.1 General
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          For the service to work, AIM must process the personal data you add to
          the services. This includes administering our services and our
          relationship with customers, providing the decision support, securing
          the quality and developing the Services and communicating and
          providing customer support, as further explained below.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Consent for processing personal data must be obtained for the app to
          work.
        </span>
      </p>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.geq3tmgkcw8p">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.1.2 To administer the Services and our relationship with you
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM use user and IT data to administer the service and our
          relationship with customers. This includes setting up accounts for the
          App, troubleshooting, and system testing, as well as notifying you of
          changes to the services or technical issues and reaching out to you.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis
        </span>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          : Contract, Consent, Legitimate interest in running the business,
          provide and ensure the proper function and use of the Services. Se
          also, the AIM GDPR Policy
        </span>
      </p>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.hbeh2ouyphfv">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.1.3 To provide decision support for cancer diagnostics
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM uses AI-based algorithms to provide decision support for the
          diagnosis of skin cancer; it does this by analysing the image data
          from images that are taken by the App. The image is taken by medical
          professionals on patient skin lesions.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis: Contract, Consent.
        </span>
      </p>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.55h5vo850mu8">
        <span
          className={`c8 c25`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.1.4 To secure the quality and develop the Services
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          We process your usage and account data to monitor and analyse how our
          customers engage and interact with the services so that we can secure
          the quality and develop the services to better align with usage
          patterns and preferences. While we have access to personal data for
          the purpose of analytics, the results are aggregated and stripped of
          any personal data.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis:
        </span>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          &nbsp;Contract, Consent, Legitimate interest
        </span>
      </p>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.gx2ol92wd3qc">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.1.5 To communicate with you and provide customer support
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          We will process personal data that you provide in inquiries to our
          customer support by telephone, email or through contact forms provided
          by us to communicate with you and act on complaints. What type of
          personal data we collect for this purpose depends on the nature of
          your inquiry. If you are a user, our support agents may request access
          to your user data if necessary to respond appropriately to your
          inquiry. Such access is subject to strict access controls and security
          measures to protect your integrity.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis: Contract, Consent, Legitimate interest to respond to
          your inquiries,
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.dgv8t4pn6uty">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.2 To conduct research
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM conduct research to evaluate the effectiveness and suitability of
          the App in clinical practice. We use the results of our research to
          communicate the benefits and limitations of Dermaliser to healthcare
          professionals. All our published research is subjected to peer
          <br />
          review and follows normal scientific processes, including ethical
          approval from the relevant professional bodies where required.
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          If we have consent, we may use your user data and other personal data
          that you may
          <br />
          provide, in anonymised form, for scientific studies, scientific
          articles and other research purposes as may be disclosed when your
          personal data is collected. However, personal data is anonymised and
          aggregated before any such publications are shared outside AIM.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          We may also contact you with requests to participate in specific
          research projects run by us or our business partners. AIM also
          contributes to research carried out by selected universities,
          institutions and other parties by sharing anonymised data with them.
          To avoid doubt, we do not share any Personal Data with such external
          parties.
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Finally, we may analyse sensitive data to publicly share insights
          learned from aggregated data with the purpose of increasing knowledge
          and understanding of
          <br />
          Skin cancer. This kind of publication is always based on aggregated
          anonymised data and, as such, doesn't contain any personal
          information.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis
        </span>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          : Consent
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.lz4hy9j205yp">
        <span
          className={`c8 c37`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.3 Marketing
        </span>
      </h3>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.xuolsd7ujwp">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.3.1 Marketing Communication
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM process transaction, account, contact and usage data to conduct
          internal usage analysis to create and send relevant messages about our
          products. Some Marketing Data is collected by using cookies. These
          cookies include third-party services that may collect information
          about your visits to our site for analytics, retargeting and
          conversion tracking purposes. Please see the AIM{" "}
        </span>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Cookie Policy
        </span>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          &nbsp;for further details.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful basis: Legitimate interest to market ourselves and our Services
        </span>
      </p>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.t6yrd4372ng9">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.3.2 Surveys and interviews
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          As a customer, you may be contacted and enabled to complete surveys or
          take part in interviews for marketing purposes. We will process the
          Profile Data that you provide in such surveys and interviews to
          analyse user preferences, improve and assess the effectiveness of
          marketing activities, and use it as marketing material or other
          promotional purposes as disclosed when your Personal Data is
          collected.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis
        </span>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          : Consent
        </span>
      </p>
      <h4
        className={`h4 c28`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.gu5j8pydmj6p">
        <span
          className={`c25 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.3.3 Marketing opt-out
        </span>
      </h4>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          You always have the right to opt out of receiving marketing
          communication by contacting us at{" "}
        </span>
        <span
          className={`c8 c47`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          support@aimedtech.com
        </span>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          .
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.inbe7un5s8tm">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          4.4 To comply with legal obligations
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Dermalyser is classified as a medical device intended for use as a
          decision support tool for diagnosing skin cancer by an EU Notified
          Body. This means that we are subject to medical device regulations
          which may require the collection and processing of your Personal Data.
          There are also other legal provisions that require the processing of
          your Personal Data, such as accounting and fraud prevention laws. For
          more details, see section 6.1.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful Basis
        </span>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          : Legal Obligation
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.gorb7fjwjeii">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          5. How long do AIM keep your personal data
        </span>
      </h2>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          We retain your Personal Data for as long as necessary to achieve the
          purposes set out in this Privacy Policy. In some cases, we may be
          required to continue to process your Personal Data for a longer period
          of time to comply with legal obligations (e.g. accounting or audit
          obligations) or for the establishment, exercise or defence of legal
          claims. When we no longer have a reason to keep your personal data, if
          you withdraw your consent or if you successfully request that we erase
          it, your data will be deleted.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          Lawful basis: Consent, Legal Obligation
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.r73uluj9ar7i">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          6. Disclosures of your Personal Data
        </span>
      </h2>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.awtc36j6ehmm">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          6.1 Recipients
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM never sells personal- or user data. We conduct extensive
          assessments before engaging any processor to ensure that they have
          appropriate technical and organisational measures in place that
          adequately protect your personal data. Anyone who is processing
          personal data on our behalf is bound by contractual obligations to
          keep personal data confidential and secure and to use it only for the
          purposes as instructed by us.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          AIM may share your Personal Data:
          <br />
        </span>
      </p>
      <ol
        className={`ol c6 lst-kix_d05zvzdd348b-0 start`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        start={1}>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            with our service providers that we use to support and provide our
            business, such as technical service or operation providers, to the
            extent needed to enable and provide the Services to you,
            <br />
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            with our successors, if we are involved in, e.g. a merger,
            acquisition or asset sale, giving you notice of this,
            <br />
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            with others with whom you ask us to share your personal data,
            <br />
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            AIM will provide personally identifying data in response to a
            third-party inquiry only if required by a valid legal process but
            will take all possible steps to keep your data private. AIM will
            contest the disclosure of your personal data in response to a
            third-party inquiry to the extent that a reasonable ground for
            objection exists. AIM will provide you with prompt prior notice of
            such a request, to the extent legally permitted, so that an order
            for relief may be requested. Suppose AIM reasonably determines that
            such disclosure is still legally required. In that case, it will
            seek a confidentiality designation protecting the disclosure. It
            will only disclose the portion necessary and at the required time,
            and/or
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            to protect and defend AIM, our business partners’ or users' rights
            and interests.
          </span>
        </li>
      </ol>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          If you choose to share your Personal Data with any third person (e.g.
          a partner), you accept that you have done so at your own risk.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.iuxw7ur5kbyb">
        <span
          className={`c8 c54`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          6
        </span>
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          . How we protect your Personal Data
        </span>
      </h2>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          All information you provide to us is transferred using encryption
          (HTTPS) and stored on secure servers. We use generally accepted
          industry standards, technologies, procedures and methods, such as
          firewalls, encrypted storage, regular software updates, security
          scans, access control, audit logging and review of admin actions as
          well as external penetration tests to protect the integrity of your
          personal data and to prevent unauthorised access. We also have
          policies and other organisational measures in place, including
          recurrent employee training on data protection and strict procedures
          to deal with any suspected personal data breach.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.g7201dc2hs0n">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          7. Third-party links
        </span>
      </h2>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          The AIM Website may contain links to other websites. Please note that
          we do not accept any responsibility or liability for personal data
          that may be collected through these websites or services. We recommend
          that you read their privacy policies before you submit any personal
          data to them or use their services.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.7blxufhl9l9o">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          8. Your rights in relation to your personal data
        </span>
      </h2>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.3xur2wt9zsz">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          8.1 Your rights
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          You have the right to:
        </span>
      </p>
      <ol
        className={`ol c6 lst-kix_9h8b72k3jdr0-0 start`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        start={1}>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            request access to and information about your Personal Data that is
            being processed by us.
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            request correction of your personal data if it is inaccurate or
            incomplete, including providing additional data if relevant
            information is missing,
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            request the erasure of your personal data,
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            object to our processing of your personal data (i) if the processing
            is based on our legitimate interest, or (ii) for direct marketing
            purposes,
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            request that we restrict the processing of all or some of your
            Personal Data in certain situations and to ask us not to send you
            any direct marketing, and
          </span>
        </li>
        <li
          className={`li c5 c17 li-bullet-0`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c4`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            request a copy of your personal data in a structured, commonly used
            format and that we transfer your personal data to another
            controller.
          </span>
        </li>
      </ol>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          If you have any concerns regarding our processing of your Personal
          Data, you have the right to file a complaint with the Swedish Data
          Protection Authority (Sw. Integritetsskyddsmyndigheten), or your local
          supervisory authority.
        </span>
      </p>
      <h3
        className={`h3 c14`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.8qnylucknc3c">
        <span
          className={`c37 c8`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          8.2 How to exercise your rights
        </span>
      </h3>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          You may contact us in writing at any time to exercise your rights,
          preferably using the email address that is associated with your user
          account. We may need to request specific information from you to help
          us confirm your identity.
        </span>
      </p>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          We do our best to respond to your request within a few days and at
          least one (1) month. If the request is complicated or if we have
          received a large number of requests, we may need to prolong our
          response time by one (1) additional month.
        </span>
      </p>
      <p
        className={`p c5`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          You can exercise your rights at no cost to you. However, we may charge
          you a reasonable fee if your request is clearly unfounded, repetitive
          or excessive.
        </span>
      </p>
      <h2
        className={`h2 c23 c29`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}
        id="h.e7jkq75l62dx">
        <span
          className={`c34`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          9. Glossary
        </span>
      </h2>
      <a id="t.e12cac525464179b6932b953027f8197e43f89e2" />
      <a id="t.1" />
      <table
        className={`c58`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <tbody>
          <tr
            className={`c21`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Anonymized data
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Anonymised data means that the identifying information is
                  irreversibly removed so that an individual is not
                  identifiable.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c44`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Application
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Dermalyser
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c57`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Consent
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c8 c10`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Consent means that you have agreed to our processing of your
                  personal data for a specific purpose by a statement or clear
                  opt-in. You can withdraw your consent anytime by contacting us
                  at support@aimedtech.com or by following the instructions
                  provided when the consent was collected.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c45`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Contract
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Contract means that the processing of your Personal Data is
                  necessary for the performance of a contract to which you are a
                  party, e.g., our Terms of Use, a purchase agreement, or to
                  take steps at your request before entering into a contract.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c41`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Legal obligation
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Legal obligation means that the processing of your Personal
                  Data is necessary for compliance with a legal obligation that
                  we are bound by, e.g., medical device regulations or national
                  laws.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c26`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Legitimate interest
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Legitimate interest means that we assess that we have a
                  legitimate interest in conducting and managing our business
                  that, considering and balancing any potential impact on you
                  and your rights, we do not consider are overridden by the
                  impact on you. Please contact us if you want to know more
                  about how we have conducted this balance of interest.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c15`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Minimised data
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Minimised data means that only the minimal amount of data
                  needed for a certain kind of processing is included.
                </span>
              </p>
            </td>
          </tr>
          <tr
            className={`c35`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            <td
              className={`c13`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c1`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Pseudonymised data
                </span>
              </p>
            </td>
            <td
              className={`c7`
                .split(" ")
                .map((c) => styles[c])
                .join(" ")}
              colSpan={1}
              rowSpan={1}>
              <p
                className={`p c2`
                  .split(" ")
                  .map((c) => styles[c])
                  .join(" ")}>
                <span
                  className={`c10 c8`
                    .split(" ")
                    .map((c) => styles[c])
                    .join(" ")}>
                  Pseudonymised data means that identifying information is
                  replaced with something else so that additional information is
                  needed to re-identify an individual. Pseudonymisation is a
                  security measure.
                </span>
              </p>
            </td>
          </tr>
        </tbody>
      </table>
      <p
        className={`p c5 c9`
          .split(" ")
          .map((c) => styles[c])
          .join(" ")}>
        <span
          className={`c4`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}
        />
      </p>
      <div>
        <p
          className={`p c33`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            className={`c8 c27`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            _________________________________________________________________________________________________________________
          </span>
        </p>
        <p
          className={`p c33`
            .split(" ")
            .map((c) => styles[c])
            .join(" ")}>
          <span
            style={{
              overflow: "hidden",
              display: "inline-block",
              margin: "0px 0px",
              border: "0px solid #000000",
              transform: "rotate(0rad) translateZ(0px)",
              WebkitTransform: "rotate(0rad) translateZ(0px)",
              width: "21.5px",
              height: 20,
            }}>
            <img
              alt=""
              src="images/logo.png"
              style={{
                width: "21.5px",
                height: 20,
                marginLeft: 0,
                marginTop: 0,
                transform: "rotate(0rad) translateZ(0px)",
                WebkitTransform: "rotate(0rad) translateZ(0px)",
              }}
              title=""
            />
          </span>
          <span
            className={`c52`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;AI Medical Technology AIM AB 2023.
            All rights reserved. &nbsp;{" "}
          </span>
          <span
            className={`c42`
              .split(" ")
              .map((c) => styles[c])
              .join(" ")}>
            &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
            &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
            &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
            &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
            &nbsp; &nbsp; &nbsp;
          </span>
        </p>
      </div>
    </div>
  );
}
